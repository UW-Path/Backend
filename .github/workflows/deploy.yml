name: remote ssh command
on: [push]
jobs:
  build:
    name: Build
    runs-on: self-hosted
    steps:
    - uses: actions/checkout@v3
    - run: |
        docker network ls|grep uwpath_backend_network > /dev/null || docker network create uwpath_backend_network
        docker-compose up
    
